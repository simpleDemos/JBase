spring.datasource.url: jdbc:mysql://1coding.online:3306/spring_jpa?useUnicode=true&characterEncoding=utf8
spring.datasource.username: root
spring.datasource.password: 123456
spring.datasource.driver-class-name: com.mysql.jdbc.Driver

#mybatis.typeAliasesPackage：为实体对象所在的包，跟数据库表一一对应
#mybatis.mapperLocations：mapper文件的位置

# 配置 mpper接口的地方
mybatis.typeAliasesPackage: com.step.demo.model
mybatis.mapperLocations: classpath:mapper/*Mapper.xml

#写法不同，不过结果一样
#mybatis.type-aliases-package=com.test.dailyreport.bean
#mybatis.mapper-locations=classpath:mapper/*Mapper.xml